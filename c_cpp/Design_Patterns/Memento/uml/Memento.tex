\documentclass[tikz]{standalone}
\usepackage{tikz}
\usepackage[AutoFakeBold=true,AutoFakeSlant=true]{xeCJK}
\usepackage[zihao=-4,UTF8,heading=true]{ctex}
\usepackage[simplified]{pgf-umlcd}
\usetikzlibrary{fit} %形状
\usetikzlibrary{positioning} %不加方向运算可能出错
\usetikzlibrary{arrows.meta} %箭头
\usetikzlibrary{calc}

\setCJKmainfont{微软雅黑}
\begin{document}
	\thispagestyle{empty}
    \begin{tikzpicture}[show background grid]
        \begin{class}[text width=5cm]{Memento}{0,0}
            \attribute{- state : string }
            \operation{+ getState(): string }
            \operation{+ setState(string stt) : void }
        \end{class}
        \begin{class}[]{Originator}{7, 0}
            \attribute{- state : string }
            \operation{+ ctMeneto() : Memento}
            \operation{+ restore(m : Memento) : void}
        \end{class}
        \begin{class}[]{Caretaker}{2,-5}
            \attribute{- memList : list<Memento>}
            \operation{+ addMem(m : Memento) : void}
            \operation{+ rmMem(m : Memento) : void}
        \end{class}
        \draw [dashed, ->] (Originator) -- (Memento);
        \aggregation{Caretaker}{}{}{Memento}

    \end{tikzpicture}

\end{document}